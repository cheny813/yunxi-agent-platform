# ============================================================
# 统一消息网关配置
# ============================================================
# 
# 【配置说明】
# 此配置文件为模块开发测试使用。生产环境请使用 agent-config 模块。
#
# ============================================================

# 导入集中化配置（来自 agent-config 模块）
spring:
  config:
    import: optional:classpath:config/gateway.yml
